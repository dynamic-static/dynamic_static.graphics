
cmake_minimum_required(VERSION 3.2)
project(${PACKAGE_NAME}-stage NONE)
include(ExternalProject)
ExternalProject_Add(
    ${PACKAGE_NAME}-stage
    ${FARGS_UNPARSED_ARGUMENTS}
    SOURCE_DIR "${PACKAGE_SOURCE_DIR}"
    # BINARY_DIR "${PACKAGE_BINARY_DIR}"
    CONFIGURE_COMMAND "" # "${CMAKE_COMMAND}" "-H${PACKAGE_SOURCE_DIR}" "-B${PACKAGE_BINARY_DIR}"
    INSTALL_COMMAND ""
    BUILD_COMMAND ""
    TEST_COMMAND ""
)

message("== PACKAGE_SOURCE_DIR ${PACKAGE_SOURCE_DIR}")

# add_subdirectory(
#     "${PACKAGE_STAGE_DIR}"
#     "${PACKAGE_BINARY_DIR}"
# )
